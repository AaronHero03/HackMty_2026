# Dependencias SOLO del servidor de la API (las instala el Dockerfile en Railway).
# Para trabajar en Colab, Windows, Ubuntu o Arch se sigue usando requirements.txt.
# No incluye torch, silero-vad ni matplotlib directo: la API no los usa y harían la imagen mucho más pesada.

numpy
scipy
soundfile
librosa
pandas
catboost                 # modelo de la API: models/modelo_catboost_altur.cbm
pymongo
fastapi
uvicorn
